#include<stdio.h>
#include<math.h>

int main(void)
{
	int *ptr = NULL;			// NULL - 0 address

	printf("sizeof(ptr) = %ld\n", sizeof(ptr));

	printf("sqrt(49) = %ld\n", sqrt(49));
	return 0;
}































